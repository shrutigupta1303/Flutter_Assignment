import 'package:flutter/material.dart';
import 'package:neostore_app/Dashboard_screens/slider_page.dart';

class DiscountPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // appBar: AppBar(
      //   title: Text("DiscountPage"),
      // ),
      body: SliderPage(),
    );
  }
}